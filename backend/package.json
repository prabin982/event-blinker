{
  "name": "event-blinker-backend",
  "version": "1.0.0",
  "description": "Event Blinker Backend API",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node scripts/migrate.js && node server.js",
    "dev": "nodemon server.js",
    "migrate": "node scripts/migrate.js",
    "build": "echo \"No build step required\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "pg-promise": "^11.4.0",
    "socket.io": "^4.5.4"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "nodemon": "^2.0.20"
  }
}